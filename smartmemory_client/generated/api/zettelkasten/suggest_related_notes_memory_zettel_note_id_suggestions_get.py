# Auto-generated by SmartMemory client generator
# DO NOT EDIT - Regenerate using: ./scripts/generate_client.sh
# Generated: 2025-11-07 13:02:52 UTC

from http import HTTPStatus
from typing import Any

import httpx

from ... import errors
from ...client import AuthenticatedClient, Client
from ...models.http_validation_error import HTTPValidationError
from ...types import UNSET, Response, Unset


def _get_kwargs(
    note_id: str,
    *,
    count: int | Unset = 5,
) -> dict[str, Any]:
    params: dict[str, Any] = {}

    params["count"] = count

    params = {k: v for k, v in params.items() if v is not UNSET and v is not None}

    _kwargs: dict[str, Any] = {
        "method": "get",
        "url": f"/memory/zettel/{note_id}/suggestions",
        "params": params,
    }

    return _kwargs


def _parse_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Any | HTTPValidationError | None:
    if response.status_code == 200:
        response_200 = response.json()
        return response_200

    if response.status_code == 422:
        response_422 = HTTPValidationError.from_dict(response.json())

        return response_422

    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    else:
        return None


def _build_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Response[Any | HTTPValidationError]:
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=_parse_response(client=client, response=response),
    )


def sync_detailed(
    note_id: str,
    *,
    client: AuthenticatedClient | Client,
    count: int | Unset = 5,
) -> Response[Any | HTTPValidationError]:
    """Suggest Related Notes

     Suggest related notes for serendipitous discovery.

    Returns notes that might be relevant but aren't directly linked,
    enabling discovery of unexpected connections.

    Args:
        note_id (str):
        count (int | Unset): Number of suggestions Default: 5.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[Any | HTTPValidationError]
    """

    kwargs = _get_kwargs(
        note_id=note_id,
        count=count,
    )

    response = client.get_httpx_client().request(
        **kwargs,
    )

    return _build_response(client=client, response=response)


def sync(
    note_id: str,
    *,
    client: AuthenticatedClient | Client,
    count: int | Unset = 5,
) -> Any | HTTPValidationError | None:
    """Suggest Related Notes

     Suggest related notes for serendipitous discovery.

    Returns notes that might be relevant but aren't directly linked,
    enabling discovery of unexpected connections.

    Args:
        note_id (str):
        count (int | Unset): Number of suggestions Default: 5.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Any | HTTPValidationError
    """

    return sync_detailed(
        note_id=note_id,
        client=client,
        count=count,
    ).parsed


async def asyncio_detailed(
    note_id: str,
    *,
    client: AuthenticatedClient | Client,
    count: int | Unset = 5,
) -> Response[Any | HTTPValidationError]:
    """Suggest Related Notes

     Suggest related notes for serendipitous discovery.

    Returns notes that might be relevant but aren't directly linked,
    enabling discovery of unexpected connections.

    Args:
        note_id (str):
        count (int | Unset): Number of suggestions Default: 5.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[Any | HTTPValidationError]
    """

    kwargs = _get_kwargs(
        note_id=note_id,
        count=count,
    )

    response = await client.get_async_httpx_client().request(**kwargs)

    return _build_response(client=client, response=response)


async def asyncio(
    note_id: str,
    *,
    client: AuthenticatedClient | Client,
    count: int | Unset = 5,
) -> Any | HTTPValidationError | None:
    """Suggest Related Notes

     Suggest related notes for serendipitous discovery.

    Returns notes that might be relevant but aren't directly linked,
    enabling discovery of unexpected connections.

    Args:
        note_id (str):
        count (int | Unset): Number of suggestions Default: 5.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Any | HTTPValidationError
    """

    return (
        await asyncio_detailed(
            note_id=note_id,
            client=client,
            count=count,
        )
    ).parsed
