# Auto-generated by SmartMemory client generator
# DO NOT EDIT - Regenerate using: ./scripts/generate_client.sh
# Generated: 2025-11-07 13:02:52 UTC

from http import HTTPStatus
from typing import Any

import httpx

from ... import errors
from ...client import AuthenticatedClient, Client
from ...models.http_validation_error import HTTPValidationError
from ...types import UNSET, Response, Unset


def _get_kwargs(
    note_id: str,
    *,
    depth: int | Unset = 2,
    include_metadata: bool | Unset = True,
) -> dict[str, Any]:
    params: dict[str, Any] = {}

    params["depth"] = depth

    params["include_metadata"] = include_metadata

    params = {k: v for k, v in params.items() if v is not UNSET and v is not None}

    _kwargs: dict[str, Any] = {
        "method": "get",
        "url": f"/memory/zettel/{note_id}/graph",
        "params": params,
    }

    return _kwargs


def _parse_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Any | HTTPValidationError | None:
    if response.status_code == 200:
        response_200 = response.json()
        return response_200

    if response.status_code == 422:
        response_422 = HTTPValidationError.from_dict(response.json())

        return response_422

    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    else:
        return None


def _build_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Response[Any | HTTPValidationError]:
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=_parse_response(client=client, response=response),
    )


def sync_detailed(
    note_id: str,
    *,
    client: AuthenticatedClient | Client,
    depth: int | Unset = 2,
    include_metadata: bool | Unset = True,
) -> Response[Any | HTTPValidationError]:
    """Get Subgraph

     Get subgraph around a note (for visualization).

    Returns nodes and edges within specified depth for
    graph visualization.

    Args:
        note_id (str):
        depth (int | Unset): Depth of subgraph (1-3) Default: 2.
        include_metadata (bool | Unset): Include note metadata Default: True.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[Any | HTTPValidationError]
    """

    kwargs = _get_kwargs(
        note_id=note_id,
        depth=depth,
        include_metadata=include_metadata,
    )

    response = client.get_httpx_client().request(
        **kwargs,
    )

    return _build_response(client=client, response=response)


def sync(
    note_id: str,
    *,
    client: AuthenticatedClient | Client,
    depth: int | Unset = 2,
    include_metadata: bool | Unset = True,
) -> Any | HTTPValidationError | None:
    """Get Subgraph

     Get subgraph around a note (for visualization).

    Returns nodes and edges within specified depth for
    graph visualization.

    Args:
        note_id (str):
        depth (int | Unset): Depth of subgraph (1-3) Default: 2.
        include_metadata (bool | Unset): Include note metadata Default: True.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Any | HTTPValidationError
    """

    return sync_detailed(
        note_id=note_id,
        client=client,
        depth=depth,
        include_metadata=include_metadata,
    ).parsed


async def asyncio_detailed(
    note_id: str,
    *,
    client: AuthenticatedClient | Client,
    depth: int | Unset = 2,
    include_metadata: bool | Unset = True,
) -> Response[Any | HTTPValidationError]:
    """Get Subgraph

     Get subgraph around a note (for visualization).

    Returns nodes and edges within specified depth for
    graph visualization.

    Args:
        note_id (str):
        depth (int | Unset): Depth of subgraph (1-3) Default: 2.
        include_metadata (bool | Unset): Include note metadata Default: True.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[Any | HTTPValidationError]
    """

    kwargs = _get_kwargs(
        note_id=note_id,
        depth=depth,
        include_metadata=include_metadata,
    )

    response = await client.get_async_httpx_client().request(**kwargs)

    return _build_response(client=client, response=response)


async def asyncio(
    note_id: str,
    *,
    client: AuthenticatedClient | Client,
    depth: int | Unset = 2,
    include_metadata: bool | Unset = True,
) -> Any | HTTPValidationError | None:
    """Get Subgraph

     Get subgraph around a note (for visualization).

    Returns nodes and edges within specified depth for
    graph visualization.

    Args:
        note_id (str):
        depth (int | Unset): Depth of subgraph (1-3) Default: 2.
        include_metadata (bool | Unset): Include note metadata Default: True.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Any | HTTPValidationError
    """

    return (
        await asyncio_detailed(
            note_id=note_id,
            client=client,
            depth=depth,
            include_metadata=include_metadata,
        )
    ).parsed
